# Python dependencies for the EBA Protokoll Cowork plugin.
# Install once per environment with:
#   pip install -r scripts/requirements.txt

# Required — DOCX generation:
python-docx>=1.0.0

# Optional — Windows-only, enables MS Word COM PDF conversion (preferred on
# Windows when MS Word is installed). Without this AND without LibreOffice,
# the PDF step is skipped and only the DOCX is produced.
pywin32>=306; platform_system == "Windows"
