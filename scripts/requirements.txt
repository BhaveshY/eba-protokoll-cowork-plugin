# Python dependencies for the EBA Protokoll Cowork plugin.
#
# End users do not install this manually. scripts/render_protokoll.py reads this
# file and bootstraps the packages in the current user environment when needed.
# Keep this list small so first-run setup remains fast in Claude Code.

# Required — DOCX generation:
python-docx>=1.0.0

# Required — official LP1-4/BIM Excel tracking output:
openpyxl>=3.1.0

# Windows-only — enables MS Word COM PDF conversion. On Windows 11 with MS Word
# installed, the renderer treats PDF as a required deliverable and auto-installs
# this package if it is missing.
pywin32>=306; platform_system == "Windows"
